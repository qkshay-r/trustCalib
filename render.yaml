services:
  - type: web
    name: trust-calib
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node --loader tsx server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20.11.0
